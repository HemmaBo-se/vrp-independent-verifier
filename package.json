{
  "name": "vrp-independent-verifier",
  "version": "1.0.0",
  "description": "Independent verifier implementation for the Vacation Rental Protocol (VRP)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node verify-test.mjs",
    "test:receipt": "node verify-receipt-test.mjs",
    "test:all": "npm test && npm run test:receipt"
  }
}
